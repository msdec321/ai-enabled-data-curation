# The "-python" variant bundles a Python interpreter, required for runCode with
# language="python". Use the plain ":0.11.0" tag if you only need exec/JS.
FROM docker.io/cloudflare/sandbox:0.11.0-python
EXPOSE 8080
